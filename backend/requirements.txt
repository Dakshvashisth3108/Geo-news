# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.32.1

# --- Data validation & settings ---
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Database (async SQLAlchemy 2.0 + Postgres) ---
SQLAlchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# --- Auth / security ---
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# --- WebSockets ---
websockets==13.1

# --- NLP ---
# Pin torch to the CPU build to keep the deploy slug small. On Render the
# default index already serves CPU wheels for linux/x86_64; if you switch
# providers, prefer:
#   pip install --extra-index-url https://download.pytorch.org/whl/cpu torch==2.5.1
torch==2.5.1
transformers==4.46.3
spacy==3.7.6
# spaCy model is installed as a separate post-install step:
#   python -m spacy download en_core_web_sm

# --- Cache ---
# Optional. Service runs fine without Redis (cache becomes a no-op).
redis[hiredis]==5.2.1

# --- Utilities ---
python-multipart==0.0.20
httpx==0.28.1
